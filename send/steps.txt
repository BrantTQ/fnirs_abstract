# Create a plain-text SOP file for the assistant, LSL-only (no TTL section)
content = """fNIRS ENEM Task — Setup & Run Guide (Assistants)

1) Get the files
Source folder: S:\\Echange\\thiago\\material
Copy these three files to your working folder (e.g., C:\\Projects\\FNIRS\\psychopy\\):
- run_enem.py
- filtered_questions.json
- demographics_questions.json
After copying, remove old versions of these files in your folder to avoid confusion.

2) Configure the script
Open run_enem.py in a text editor and update:

a) Path to the question bank
# change this to the path where YOU put filtered_questions.json
QUESTIONS_JSON = r"C:\\Your\\Path\\To\\filtered_questions.json"
(If demographics_questions.json isn’t in the same folder as the script, also update SOCIO_Q_FILE similarly.)

b) fNIRS markers (LSL with NIRSport2/Aurora)
USE_FNIRS = True
USE_LSL   = True
USE_TTL   = False
Do not forget USE_FNIRS = True; otherwise no markers are sent.

3) Create/activate the Conda environment
Open Anaconda Prompt.

- Create (first time only):
conda create -n fnirs python=3.10 -y

- Activate:
conda activate fnirs

- Install PsychoPy (first time only):
pip install psychopy

- Install LSL (if needed):
pip install pylsl


3) Open Aurora

4) Connect to the device

5) un the task
From Anaconda Prompt:
python C:\\Your\\Path\\To\\run_enem.py
(Replace with your actual path.)

Quit: Press Esc in the task window.
If that fails, focus the Anaconda Prompt window and press Alt + F4.

6) hat gets saved & where
Logs are written to a logs subfolder next to the script:
<your working folder>\\logs\\
- enem_blocks_<participant>_<timestamp>.csv — trial/block log
- socio_<participant>_<timestamp>.json — questionnaire answers

7) uick checks / troubleshooting
- Black/blank window: ensure GPU drivers are up to date; try FULLSCREEN = False.
- Questions don’t load: confirm QUESTIONS_JSON path is correct and the JSON is valid.
- Questionnaire doesn’t appear: ensure RUN_QUESTIONNAIRE_BEFORE = True.
- LSL markers not visible: confirm pylsl installed and check with LSL Inspector/LabRecorder for stream 'psychopy_markers' (type 'Markers').
"""

